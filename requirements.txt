# ============================================================
# 知识中台提效助手 - 内网部署依赖
# Python 3.11 / 3.12 / 3.13
# 安装: pip install -r requirements.txt
# ============================================================

# ---------- Web 框架 ----------
fastapi==0.139.0
uvicorn[standard]==0.51.0
sse-starlette==3.4.5          # SSE 流式对话
python-multipart==0.0.32      # 文档上传 (multipart)

# ---------- LLM / Agent 框架 ----------
langchain==1.3.12
langchain-core==1.4.9
langchain-openai==1.3.4       # OpenAI 兼容接口 (SiliconFlow 等)
langchain-deepseek==1.1.0     # DeepSeek 专用接口 (可选)
langchain-qwq==0.3.5         # 通义千问 (可选)
langgraph==1.2.8              # Agent + 多轮对话编排
langgraph-checkpoint==4.1.1   # 会话记忆 (MemorySaver)
langgraph-prebuilt==1.1.0     # create_agent 依赖
openai==2.44.0                # OpenAI SDK (langchain-openai 底层依赖)

# ---------- 配置 / 工具 ----------
pydantic==2.13.4
pydantic-settings==2.14.2     # .env 配置加载
httpx==0.28.1                 # 异步 HTTP (调用 KB API)
loguru==0.7.3                 # 日志
python-dotenv==1.2.2          # .env 解析

# ============================================================
# 可选: 会话持久化 (默认用内存 memory, 若改用 PostgreSQL 再装)
# 启用方式: .env 中 SESSION_CHECKPOINT_BACKEND=postgres
# ============================================================
# langgraph-checkpoint-postgres==3.1.0
# psycopg[binary,pool]==3.3.4

# ============================================================
# 可选: AIOps / MCP 模块 (预留, 默认不启用)
# 启用方式: 启动 mcp_servers/ 下的服务
# ============================================================
# langchain-mcp-adapters==0.3.0
# fastmcp==3.4.4

# ============================================================
# 以下为已废弃依赖, 无需安装 (RAG 检索已改用内网 KB API)
#   pymilvus / langchain-milvus   - 本地向量库 (已不需要)
#   dashscope                      - 旧 Embedding (已不需要)
#   langchain-text-splitters       - 文档分块 (已不需要)
#   langchain-community            - 旧社区集成
#   aiohttp / aiofiles             - 未直接使用
# ============================================================