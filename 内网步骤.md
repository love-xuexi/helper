# 1. 创建虚拟环境 (Python 3.11-3.13)
#    start-windows.bat 会自动创建，也可手动：
python -m venv .venv
.\.venv\Scripts\Activate.ps1

# 2. 安装依赖 (离线时需先在有网机器 pip download 打包)
#    start-windows.bat 会自动安装，也可手动：
pip install -r requirements.txt
#    依赖装好后会生成 .venv/.deps_stamp，后续启动自动跳过安装
#    强制重装：删除 .venv/.deps_stamp 后重新启动

# 3. 配置 .env (核心项)
#    CHAT_API_KEY     - LLM Key
#    CHAT_BASE_URL    - https://api.deepseek.com/v1  (注意不要带 /chat/completions)
#    CHAT_PROVIDER    - deepseek
#    KB_RETRIEVAL_BASE_URL - 内网 KB API 地址

# 4. 启动
.\start-windows.bat
#    首次启动约 1-2 分钟（创建 venv + 安装依赖）
#    后续启动约 5-8 秒（跳过依赖安装，隐藏启动，无弹窗）
#    启动脚本会轮询健康检查，服务就绪后自动结束

# ============================================================
# 启动后使用
# ============================================================

# 5. 访问地址
#    Web UI (对话界面):  http://localhost:9983
#    管理后台:           http://localhost:9983/admin
#    API 文档 (Swagger): http://localhost:9983/docs
#    健康检查:           http://localhost:9983/api/health

# 6. 核心接口
#    POST /api/chat          - 非流式问答（一次性返回答案 + 引用来源）
#    POST /api/chat_stream   - 流式问答（SSE，含检索状态和流式内容）
#    POST /api/chat/clear    - 清空会话历史
#    GET  /api/chat/sessions - 列出所有会话（分页，按更新时间倒序）
#    GET  /api/chat/session/{id}    - 获取指定会话的完整对话历史
#    PUT  /api/chat/session/{id}/rename - 重命名会话标题
#    GET  /api/chat/suggestions - 获取推荐问题（基于点赞反馈）
#    POST /api/upload        - 上传文档到知识库
#    POST /api/feedback      - 提交反馈评价（点赞/不喜欢 + 结构化负反馈）
#    GET  /api/feedback      - 反馈列表（分页，支持 rating 过滤）
#    GET  /api/feedback/stats - 反馈统计（点赞率、标签分布）
#    GET  /api/feedback/tags   - 获取预设负反馈标签
#    GET  /api/feedback/message/{message_id} - 查询某条回答的反馈状态
#
#    Bug 上报与管理：
#    POST /api/bug/report    - 上报 Bug（multipart，支持附件）
#    GET  /api/bug/list      - Bug 列表（支持状态/分类过滤、分页）
#    GET  /api/bug/{id}      - 获取单条 Bug 详情
#    PUT  /api/bug/{id}/status - 更新 Bug 状态（待处理/处理中/已解决/已关闭）
#    GET  /api/bug/stats     - Bug 统计（总数、状态分布、分类分布）
#    GET  /api/bug/categories - 获取预设分类列表
#    GET  /api/admin/timeline - Bug + 反馈合并时间线（管理后台用）

# ============================================================
# 管理后台
# ============================================================

# 7. 管理后台地址：http://localhost:9983/admin
#    也可在 Web UI 左下角点击「管理后台」按钮打开
#
#    功能：
#    - 🐞 Bug 列表：查看所有上报的 Bug，支持按状态/分类筛选，可更新状态、查看详情
#    - 👍 反馈记录：查看所有点赞/点踩反馈，含负反馈标签和详细描述
#    - 💬 会话列表：查看所有对话会话，点击查看完整对话历史
#    - 📊 统计仪表盘：Bug 总数、待处理数、点赞/点踩数一览
#
#    对话界面中的 Bug 上报：
#    - 点击输入框左侧「⋯」按钮 → 选择「上报Bug」
#    - 填写分类、标题、详细描述，可上传附件（截图/日志，最大 20MB）
#    - 系统会自动关联当前会话 ID 和最近的问答内容

# ============================================================
# 日志查看
# ============================================================

# 8. 日志文件（启动后自动在 logs/ 子目录生成，按时间戳命名，不覆盖旧日志）
#
#    logs/server/server_YYYYMMDD_HHMMSS.log       - uvicorn 启动日志 + stdout（运行时控制台输出）
#    logs/server/server_error_YYYYMMDD_HHMMSS.log - stderr（未捕获异常、崩溃栈，启动失败时优先看这个）
#    logs/app/app_YYYY-MM-DD.log                   - loguru 结构化日志（按天轮转，保留 7 天，自动压缩）
#    server.pid          - API 进程 PID（供 stop-windows.bat 精准停止）
#
#    数据文件（首次使用后自动创建）：
#    data/feedback.db       - 反馈评价数据库（SQLite）
#    data/bug.db            - Bug 上报数据库（SQLite）
#    data/chat_sessions.db  - 会话历史数据库（SQLite）
#    data/uploads/          - Bug 附件存储目录

# 9. 实时查看日志（跟踪运行状态）
#    跟踪最新一次启动的 stdout 日志（取 logs/server/ 下最新的 server_*.log）：
powershell -NoProfile -Command "Get-Content -Path (Get-ChildItem 'logs\server\server_*.log' | Sort-Object LastWriteTime -Descending | Select-Object -First 1).FullName -Wait -Tail 80 -Encoding UTF8"
#    跟踪 loguru 结构化日志：
powershell -NoProfile -Command "Get-Content -Path 'logs\app\app_$(Get-Date -Format yyyy-MM-dd).log' -Wait -Tail 80 -Encoding UTF8"

# 10. 查看历史日志（排查过去的问题）
#     logs/app/ 目录下按天存放，如 logs/app/app_2026-07-13.log
#     超过 7 天的日志会自动压缩为 .zip

# ============================================================
# 停止与重启
# ============================================================

# 11. 停止服务
.\stop-windows.bat
#     通过 server.pid 精准停止 API 进程（无弹窗残留）

# 12. 重启服务
.\stop-windows.bat
.\start-windows.bat

# ============================================================
# 可选：MCP 服务（AIOps 智能运维模块）
# ============================================================

# 13. MCP 服务默认不启动，如需 AIOps 功能，另开终端手动启动：
#     CLS MCP 服务 (端口 8003):
.\.venv\Scripts\python.exe mcp_servers\cls_server.py
#     Monitor MCP 服务 (端口 8004):
.\.venv\Scripts\python.exe mcp_servers\monitor_server.py
#     启动后可用 .\stop-windows.bat 一并停止

# ============================================================
# 常见问题
# ============================================================

# 14. 启动失败排查顺序：
#     a) 看终端输出的 [ERROR] 行
#     b) 看 logs/ 下最新的 server_error_*.log（stderr，崩溃栈）
#     c) 看 logs/ 下最新的 server_*.log（uvicorn 启动日志）
#     d) 看 logs/app/app_*.log（loguru 详细日志）
#
# 15. 依赖损坏 / import 失败：
#     删除 .venv 目录后重新运行 start-windows.bat（全自动重建）
#
# 16. 端口 9983 被占用：
#     运行 stop-windows.bat 停止旧进程，或手动查杀：
netstat -ano | findstr :9983
#     然后 taskkill /PID <PID> /F
